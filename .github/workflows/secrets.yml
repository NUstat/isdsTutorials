name: secrets

on:
  schedule:
    - cron: '*/15 * * * *' # runs every 15 min


jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Retrieve secret
        env:
          secret_email: ${{ secrets.SECRET_EMAIL }}
        run: |
          example-command "$secret_email"
