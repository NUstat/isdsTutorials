---
title: "Tutorial: Chapter 9.2 - 9.4"
tutorial:
  id: "isds_11"
  version: 0.1
  
output:
  learnr::tutorial:
    progressive: true
    allow_skip: true
    css: ["css/nu-theme.css"]
runtime: shiny_prerendered
description: >
 Learn about the sampling distribution.
---

```{r setup, include=FALSE}
library(learnr)
library(tidyverse)
library(ISDStutorials)
library(gradethis)

knitr::opts_chunk$set(echo = FALSE)
gradethis_setup()
options(tutorial.event_recorder = ISDStutorials::isds_recorder)

```


```{r, context = "server"}
rubric_list <- data.frame(question = c("Ex1", "Q1", "Q2", "Q3", "Q4", "Q5", "Q6", "Q7", "Q8", "Q9", "Q10"),
                          points_possible = rep(1, 11))

grade_server("grade_out", 
            rubric_list = rubric_list)
```


```{r isds-header}
# student name
question_blank("Name: ___",
                            answer_fn(function(value){
                              if (length(value) >= 1 ) {
                                return(mark_as(TRUE))
                                }
                              return(mark_as(FALSE) )
                              }),
               style = "notes_question", 
               allow_retry = FALSE )
# have name, grade button, and print button on same line
bootstrapPage(
     div(style="display:inline-block",
         grade_button_ui("grade_out") ),
     div(style="display:inline-block", print_ui("Print") )
)

# can have the grade output appear anywhere in your tutorial
grade_output_ui("grade_out")
```

## Instructions

Complete this tutorial while reading Sections 9.2 - 9.4 of the [textbook](https://nustat.github.io/intro-stat-ds/9-sampling.html#repeated-sampling).

Once you are finished:

-   Click the 'grade tutorial' button at the top to retrieve your score.
-   Click the 'pdf' button to save the document as a pdf, you must have the tutorial open in a browser window for this button to work.
-   Submit the completed pdf to Canvas.


### Goals

-   Understand the theory of repeated sampling.
-   Describe the properties of a sampling distribution.
-   Understand the difference between a distribution and a sampling distribution.


## Reading Quiz

```{r Q}
quiz(
  caption = NULL,
  #Question 1
  question("Which of the following are TRUE about the theory of repeated samples? Select all that apply.  ",
           answer("It is a purely theoretical construct - you will not observe repeated samples in real life", correct = TRUE),
           answer("It allows us to calculate the true value of a population parameter exactly"),
           answer("It provides the theory for how we can use a sample to infer to a population", correct = TRUE),
           answer("It relies on the process of randomization (e.g. random sampling)", correct = TRUE),
           allow_retry = TRUE,
           random_answer_order = TRUE),
  # Question 2
  question("What is the maximum value of `replicate` in the `tactile_prop_red` dataset? ", 
           type = "learnr_text",
           answer("33", correct = TRUE),
           allow_retry = TRUE),
  # Question 3
  question_blank("<strong> Q3) </strong> <br/>
  a) What does the value `TRUE` evaluate to in R? ___  <br/>
  b) What does the value `FALSE` evaluate to in R? ___", 
           answer("1", correct = TRUE),
           answer("0", correct = TRUE),
           allow_retry = TRUE),
  # Q4needed
  question(paste0("Q4) Consider the tibble `coin_flips_RQ` shown below and the lines of code that follow.\n
           coin_flips_RQ %>%  
                summarize(
                  sum = sum(result),
                  mean = mean(result)
                  )
    


",htmltools::img(src="images/coin_flips_RQ.png", height = 400, width = 200),"

What is the resulting value for `sum`?"), 
           type = "learnr_text",
           answer("3", correct = TRUE),
           allow_retry = TRUE),
  # Question 5
  # Graphic needed
  question("Consider the tibble `coin_flips_RQ` shown in the prior question and the lines of code that follow.\n
           coin_flips_RQ %>%  
                summarize(
                  sum = sum(result),
                  mean = mean(result)
                  )

What is the resulting value for `mean`? ", 
           type = "learnr_text",
           answer("0.3", correct = TRUE),
           answer(".3", correct = TRUE),
           allow_retry = TRUE),
  # Question 6
  question("Consider the data frame `virtual_samples_RQ` that results from the following lines of code:\n
           virtual_samples_RQ <- bowl %>%  
                rep_sample_n(size = 50, reps = 5000)

How many rows will `virtual_samples_RQ` have?  ", 
           type = "learnr_text",
           answer("250000", correct = TRUE),
           allow_retry = TRUE),
  # Question 7
  question("Consider the data frame `virtual_samples_RQ` that results from the following lines of code:\n
           virtual_samples_RQ <- bowl %>%  
                rep_sample_n(size = 50, reps = 5000)

What will be the maximum value of the `replicate` column?  ", 
           type = "learnr_text",
           answer("5000", correct = TRUE),
           allow_retry = TRUE),
  # Question 8
  question("Consider the data frame `virtual_samples_RQ` that results from the following lines of code:\n
           virtual_samples_RQ <- bowl %>%  
                rep_sample_n(size = 50, reps = 5000)

What will the value of `replicate` be for the 73rd row?  ", 
           type = "learnr_text",
           answer("2", correct = TRUE),
           allow_retry = TRUE),
  # Question 9
  question("Consider the data frame `virtual_samples_RQ` that results from the following lines of code:\n
           virtual_samples_RQ <- bowl %>%  
                rep_sample_n(size = 50, reps = 5000)

How many rows will have `replicate == 437`?  ", 
           type = "learnr_text",
           answer("50", correct = TRUE),
           allow_retry = TRUE),
  # Question 10 
  question("Consider the bowl of red and white balls in Figure 9.10 and its virtual analog in the data frame bowls.\n
           
How many balls total are in the bowl?", 
           type = "learnr_text",
           answer("2400", correct = TRUE),
           allow_retry = TRUE),
  # Question 11
  question("Consider the bowl of red and white balls in Figure 9.10 and its virtual analog in the data frame bowls.\n

What is the true proportion of red balls in the bowl? ", 
           type = "learnr_text",
           answer("0.375", correct = TRUE),
           allow_retry = TRUE),
  # Question 12
  question("Consider the bowl of red and white balls in Figure 9.10 and its virtual analog in the data frame bowls.\n
  
How many red balls are in the bowl?", 
           type = "learnr_text",
           answer("900", correct = TRUE),
           allow_retry = TRUE),
  # Question 13
  question("What phrase do we use to refer to the standard deviation of a sampling distribution? ", 
           type = "learnr_text",
           answer("standard error", correct = TRUE),
           allow_retry = TRUE),
  # Question 14
  question("Match the following description with the appropriate term: \n

A measure of spread of an estimator across repeated samples.", 
           answer("Variance"),
           answer("Degrees of freedom"),
           answer("Bias"),
           answer("Standard error", correct = TRUE),
           answer("The distribution of possible values of estimates across repeated samples"),
           allow_retry = TRUE),
  # Question 15
  question("Match the following description with the appropriate term: \n

Standard deviation squared; a measure of spread in the data.", 
           answer("Variance", correct = TRUE),
           answer("Degrees of freedom"),
           answer("Bias"),
           answer("Standard error"),
           answer("The distribution of possible values of estimates across repeated samples"),
           allow_retry = TRUE),
  # Question 16
  question("Match the following description with the appropriate term: \n

A parameter of the t-distribution that depends on sample size.", 
           answer("Variance"),
           answer("Degrees of freedom", correct = TRUE),
           answer("Bias"),
           answer("Standard error"),
           answer("The distribution of possible values of estimates across repeated samples"),
           allow_retry = TRUE),
  # Question 17
  question("Match the following description with the appropriate term: \n

The difference between the average value of a sampling distribution and the true population parameter.", 
           answer("Variance"),
           answer("Degrees of freedom"),
           answer("Bias", correct = TRUE),
           answer("Standard error"),
           answer("The distribution of possible values of estimates across repeated samples"),
           allow_retry = TRUE),
  # Question 18
  question("Match the following description with the appropriate term:\n

The distribution of possible values of estimates across repeated samples.", 
           answer("Variance"),
           answer("Degrees of freedom"),
           answer("Bias"),
           answer("Standard error"),
           answer("The distribution of possible values of estimates across repeated samples", correct = TRUE),
           allow_retry = TRUE)
)

```
